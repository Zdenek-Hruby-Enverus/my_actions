jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: step1
        run:
          echo "Running the first custom job"
