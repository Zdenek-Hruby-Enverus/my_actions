name: Output Action
description: simulates output for a custom action

runs:
  using: "composite"
  steps:
    - name: Output Test
      shell: bash
      run:
        CUSTOM_OP="CUSTOM ACTION RULEZ"
        echo "custom_action_output=$CUSTOM_OP"
